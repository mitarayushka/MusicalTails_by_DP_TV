<Window x:Class="MusicalTails.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ввод имени" Height="200" Width="300"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    <StackPanel Margin="10">
        <TextBlock x:Name="QuestionText" Margin="0,0,0,10" Foreground="{StaticResource TextBrush}"/>
        <TextBox x:Name="AnswerTextBox" Margin="0,0,0,10"/>
        <Button Content="OK" Click="OkButton_Click" HorizontalAlignment="Center" Padding="20,5"
                Background="{StaticResource PrimaryBrush}"
                Foreground="{StaticResource TextLightBrush}"
                BorderBrush="{StaticResource AccentBrush}"/>
    </StackPanel>
</Window>
